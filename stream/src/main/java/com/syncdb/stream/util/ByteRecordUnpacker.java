package com.syncdb.stream.util;

import com.syncdb.core.models.Record;
import lombok.extern.slf4j.Slf4j;

@Slf4j
public class ByteRecordUnpacker {
//  public static Record<byte[], byte[]> unpack( byte[] bytes) {
//    int x = p.unpackMapHeader();
//    while (--x >= 0) {
//      System.out.println(p.unpackString());
//      int bb = p.unpackBinaryHeader();
//      byte[] bytes = new byte[bb];
//      for (int i = 0; i < bb; i++) {
//        bytes[i] = p.unpackByte();
//      }
//      System.out.println(new String(bytes));
//    }
//  }
}
